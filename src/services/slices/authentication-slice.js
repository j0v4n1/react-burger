import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  accessToken: null
}

const authenticationSlice = createSlice({
  name: 'authorization',
  initialState,
  reducers: {

  }
})
